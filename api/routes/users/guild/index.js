const { Router } = require('express');

const router = Router();

router.get('/:guildId/:userId', (req, res) => {});

router.post('/:guildId/:userId', (req, res) => {});

router.delete('/:guildId/:userId', (req, res) => {});

router.put('/:guildId/:userId', (req, res) => {});

module.exports = router;
